{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "chevron-up",
  "title": "chevron-up",
  "description": "Semantic animated icon — it nods up. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/chevron-up.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInCubic, easeOutQuart } from '../core/easings'\n\n/**\n * Chevron up — it nods up. Two diminishing beats, like an eager nod: up,\n * settle, up again smaller, settle home.\n * Base geometry: Lucide `chevron-up` (ISC).\n */\nconst DUR = 0.85\n\nexport function ChevronUpIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'chevron up'}\n      {...hoverProps}\n    >\n      <motion.path\n        d=\"m18 15-6-6-6 6\"\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { y: 0 },\n          animate: {\n            y: [0, -2, 0, -1, 0, 0],\n            transition: {\n              duration: DUR,\n              times: [0, 0.2, 0.4, 0.55, 0.7, 1],\n              ease: [easeOutQuart, easeInCubic, easeOutQuart, easeInCubic, 'linear'],\n            },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'chevron-up',\n  gesture: 'it nods up',\n  family: 'rigid' as const,\n  section: 'Arrows',\n  tags: ['up', 'collapse'],\n}\n\nexport default ChevronUpIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/chevron-up.tsx"
    }
  ],
  "type": "registry:component"
}