{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "step-forward",
  "title": "step-forward",
  "description": "Semantic animated icon — it makes its move. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/step-forward.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInOutCubic, settleBack } from '../core/easings'\n\n/**\n * Step forward — it steps forward. The triangle winds up toward the stop\n * bar, touching it (the bar gets a small contact nudge), then drives\n * forward past rest in the direction it points before settling home.\n * Base geometry: Lucide `step-forward` (ISC).\n */\nconst DUR = 0.75\n\nexport function StepForwardIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'step forward'}\n      {...hoverProps}\n    >\n      <motion.path\n        d=\"M10.029 4.285A2 2 0 0 0 7 6v12a2 2 0 0 0 3.029 1.715l9.997-5.998a2 2 0 0 0 .003-3.432z\"\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { x: 0 },\n          animate: {\n            x: [0, -0.9, 1.8, 0],\n            transition: { duration: DUR, times: [0, 0.18, 0.55, 1], ease: [easeInOutCubic, easeInOutCubic, settleBack] },\n          },\n        }}\n      />\n      <motion.path\n        d=\"M3 4v16\"\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { x: 0 },\n          animate: {\n            x: [0, -0.6, 0, 0],\n            transition: { duration: DUR, times: [0, 0.18, 0.4, 1], ease: [easeInOutCubic, settleBack, 'linear'] },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'step-forward',\n  gesture: 'it makes its move',\n  family: 'rigid' as const,\n  section: 'Arrows',\n  tags: ['navigate', 'step', 'forward'],\n}\n\nexport default StepForwardIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/step-forward.tsx"
    }
  ],
  "type": "registry:component"
}