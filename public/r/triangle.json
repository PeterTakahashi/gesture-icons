{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "triangle",
  "title": "triangle",
  "description": "Semantic animated icon — it stands stable. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/triangle.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInOutCubic } from '../core/easings'\n\n/**\n * Triangle — it stands stable. A rock test: it rocks about its own base and\n * settles back, decaying fast — it cannot be tipped.\n * Base geometry: Lucide `triangle` (ISC).\n */\nconst DUR = 0.65\n\nexport function TriangleIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'triangle'}\n      {...hoverProps}\n    >\n      <motion.path\n        d=\"M13.73 4a2 2 0 0 0-3.46 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z\"\n        style={{ transformBox: 'view-box', transformOrigin: '12px 21px' }}\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { rotate: 0 },\n          animate: {\n            rotate: [0, -4, 3, -1.5, 0],\n            transition: { duration: DUR, times: [0, 0.25, 0.5, 0.75, 1], ease: easeInOutCubic },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'triangle',\n  gesture: 'it stands stable',\n  family: 'rigid' as const,\n  section: 'Shapes',\n  tags: ['shape', 'geometry'],\n}\n\nexport default TriangleIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/triangle.tsx"
    }
  ],
  "type": "registry:component"
}