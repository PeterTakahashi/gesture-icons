{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "check-square",
  "title": "check-square",
  "description": "Semantic animated icon — it draws itself. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/check-square.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInCubic, easeOutQuart, pen } from '../core/easings'\n\n/**\n * Check square — the frame takes one soft breath while the tick\n * pen-redraws inside it: erase fast, then write it back on, landing on the\n * resting glyph exactly as check.tsx and badge-check.tsx do.\n * Base geometry: Lucide `check-square` (ISC).\n */\nconst DUR = 0.95\n\nexport function CheckSquareIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'check square'}\n      {...hoverProps}\n    >\n      <motion.path\n        d=\"M21 10.656V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h12.344\"\n        style={{ transformBox: 'view-box', transformOrigin: '12px 12px' }}\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { scale: 1 },\n          animate: {\n            scale: [1, 1.03, 1],\n            transition: { duration: DUR, times: [0, 0.45, 0.85], ease: easeOutQuart },\n          },\n        }}\n      />\n      <motion.path\n        d=\"m9 11 3 3L22 4\"\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { pathLength: 1 },\n          animate: {\n            pathLength: [1, 0.001, 0.001, 1],\n            transition: { duration: DUR, times: [0, 0.22, 0.4, 0.85], ease: [easeInCubic, 'linear', pen] },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'check-square',\n  gesture: 'it draws itself',\n  family: 'draw-on' as const,\n  section: 'Interface',\n  tags: ['frame', 'mark', 'check', 'square'],\n}\n\nexport default CheckSquareIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/check-square.tsx"
    }
  ],
  "type": "registry:component"
}