{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "list-video",
  "title": "list-video",
  "description": "Semantic animated icon — it does what it means. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/list-video.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInCubic, easeOutQuart, settleBack } from '../core/easings'\n\n/**\n * List video — VARIANT(list): the rows hold; the play mark stamps once —\n * pressed then popped past its size — since it is a single glyph, not a\n * set of marks to stagger top-down.\n * Base geometry: Lucide `list-video` (ISC).\n */\nconst DUR = 0.7\n\nexport function ListVideoIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'list video'}\n      {...hoverProps}\n    >\n      <path d=\"M21 5H3\" />\n      <path d=\"M10 12H3\" />\n      <path d=\"M10 19H3\" />\n      <motion.path\n        d=\"M15 12.003a1 1 0 0 1 1.517-.859l4.997 2.997a1 1 0 0 1 0 1.718l-4.997 2.997a1 1 0 0 1-1.517-.86z\"\n        style={{ transformBox: 'view-box', transformOrigin: '18px 12px' }}\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { scale: 1 },\n          animate: {\n            scale: [1, 0.82, 1.14, 1],\n            transition: { duration: DUR, times: [0, 0.2, 0.55, 1], ease: [easeInCubic, settleBack, easeOutQuart] },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'list-video',\n  gesture: 'it does what it means',\n  family: 'rigid' as const,\n  section: 'Text & editing',\n  tags: ['list', 'video'],\n}\n\nexport default ListVideoIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/list-video.tsx"
    }
  ],
  "type": "registry:component"
}