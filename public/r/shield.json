{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "shield",
  "title": "shield",
  "description": "Semantic animated icon — it braces. Ends exactly on its resting picture.",
  "dependencies": [
    "motion"
  ],
  "registryDependencies": [
    "https://claude-code-icons.vercel.app/r/gesture-core.json"
  ],
  "files": [
    {
      "path": "src/lib/icons/shield.tsx",
      "content": "import { motion } from 'motion/react'\nimport { useGesture } from '../core/useGesture'\nimport { svgDefaults, type GestureIconProps } from '../core/types'\nimport { easeInCubic, easeOutQuart, settleBack } from '../core/easings'\n\n/**\n * Shield — it braces. One firm press about its own center — the load taken,\n * standing a hair taller after — like flexing against a hit. Ends on the\n * exact resting glyph.\n * Base geometry: Lucide `shield` (ISC).\n */\nconst DUR = 0.85\n\nexport function ShieldIcon({\n  size = 24, color = 'currentColor', strokeWidth = 2,\n  trigger, className, style, handleRef, ...rest\n}: GestureIconProps) {\n  const { controls, hoverProps } = useGesture({ trigger, handleRef })\n  return (\n    <svg\n      {...svgDefaults} width={size} height={size} stroke={color} strokeWidth={strokeWidth}\n      className={className} style={style} role=\"img\" aria-label={rest['aria-label'] ?? 'shield'}\n      {...hoverProps}\n    >\n      <motion.path\n        d=\"M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z\"\n        style={{ transformBox: 'view-box', transformOrigin: '12px 12px' }}\n        initial=\"normal\"\n        animate={controls}\n        variants={{\n          normal: { scale: 1 },\n          animate: {\n            scale: [1, 0.95, 1.04, 1],\n            transition: { duration: DUR, times: [0, 0.28, 0.6, 1], ease: [easeInCubic, settleBack, easeOutQuart] },\n          },\n        }}\n      />\n    </svg>\n  )\n}\n\nexport const meta = {\n  name: 'shield',\n  gesture: 'it braces',\n  family: 'rigid' as const,\n  section: 'Security',\n  tags: ['protect', 'defense', 'safe'],\n}\n\nexport default ShieldIcon\n",
      "type": "registry:component",
      "target": "components/gesture-icons/icons/shield.tsx"
    }
  ],
  "type": "registry:component"
}